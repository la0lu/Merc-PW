# 02 — Rationales

> Workflow: Claude writes the **Draft**; the author rewrites it in their own words as **Final** (that is what goes into Studio).
> Each Final answer must be **>200 characters**, specific, and reference real mechanisms, modules, and paths from `03-research-notes.md`.

---

## Q1 — Why do you think this is a task that frontier models would struggle with?
Formula: the subtle property (invariant / race / cross-layer span / multiple code paths / compat) + where it spans + the shortcut a model is likely to take.

### Draft (Claude)

### Final (author's words — Studio)

Characters: 

---

## Q2 — What does this codebase do?
Formula: what the system is + stack (langs, frameworks, DB, queue, cache) + rough size (verified count) + modules relevant to the task + how data/control flows.

### Draft (Claude)

### Final (author's words — Studio)

Characters: 

---

## Q3 — What issues or gaps does the codebase have, and how would a developer get started on solving them?
Formula: concrete gap (where + why it exists) + realistic first step (failing test pinning the behaviour) + direction of the fix.

### Draft (Claude)

### Final (author's words — Studio)

Characters: 
